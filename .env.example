# Public build-time values. Never put provider or mailbox secrets here.
PUBLIC_SITE_URL=https://www.example.com
PUBLIC_CONTACT_ENDPOINT=https://forms.example.com/contact

# Non-secret GitHub Actions repository variable used only after deployment.
# It is not required by `astro build`.
PRODUCTION_SITE_URL=https://www.example.com
